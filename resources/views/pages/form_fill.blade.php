@extends('layouts.app')@section('title','Create Form')@section('content')<div class="container-fluid">    <div class="card-body">        <div class="col-md-12">             @if(count($errors))            	<div class="alert alert-danger">                        		<strong>Whoops!</strong> There were some problems with your input.                        		<br/>                        		<ul>                        			@foreach($errors->all() as $error)                        			<li>{{ $error }}</li>                        			@endforeach                        		</ul>                        	</div>                        @endif            <form action="{{ route('post:form_fill') }}" method="post" enctype="multipart/form-data">                @csrf                <input type="hidden" value="{{ $form_id }}" name="form_id">                @foreach($form as $r)                    <div class="card shadow mb-3" id="fomr_1">                        <div class="card-body">                            <div class="col-md-12 mb-3">                                <label for="phone">{{ $r->feild_name }}:</label>                                <input type="{{ $r->feild_type }}" class="form-control" id="" name="{{ $r->feild_name }}"                                  placeholder="Enter {{ $r->feild_name }}" autocomplete="off">                            </div>                                                    </div>                    </div>                @endforeach                                <button type="submit" class="btn btn-primary submit-btn">Create Form</button>            </form>        </div>    </div>    </div>@endsection